2026-05-26

- Built an editable report composer for the stained glass app in `/Users/scottishgroup/Desktop/ssg-platform` with a live branded preview, editable section text, selectable cover photo, per-section gallery photo placement, shared draft plumbing, and PDF generation that respects the saved layout.
- A refresh-related dashboard breakage was caused by a corrupted Next dev cache in `dashboard/.next/`; the symptom was raw unstyled HTML because the CSS asset request failed with `Cannot find module './825.js'`. Clearing the stale `.next` directory and restarting the dashboard restored normal rendering.
- A staff-permission failure during project creation/testing came from auth token collision between the office dashboard and customer portal. The portal had been overwriting the same `ssg_token`, so testing customer flows could silently downgrade the dashboard session and trigger `Staff access required`.
- The fix separated portal auth from dashboard auth, hardened dashboard login/layout guards, and updated backend role checks so `admin` is treated as staff-equivalent where staff access is required.
- Verified after fixes: staff login with code `0000` could create projects again; customer login on an accepted project could load the project, submit notes, and generate a proposal job; `npm run build` passed in `dashboard/`; `python3 -m compileall backend/app backend/processing` passed.
- Reworked the dashboard Projects page to match the Scottish Stained Glass brand more closely: bright green sidebar and controls, checkered wordmark treatment, sticky top bar, branded status badges, stained-glass placeholder art, improved empty states, and a stronger mobile layout with chip filters and FAB behavior.
- Reworked the project detail page into a four-tab flow: `Photos & Notes`, `Estimate`, `Generate Report`, and `Customer Portal`, removing the old right-side helper panel and making the tabs mobile-friendly.
- Added the new project-detail behaviors behind that redesign: queued photo upload states with inline shorthand help, grouped estimate briefs with an `Improve with AI` action, editable report section cards with preview/download/publish gating, and customer-portal publish state controls.
- For reliable manual testing, the dashboard is currently being run from a production build on port `3000` with `next start` instead of `next dev`, because the dev server had been prone to corrupted `.next` state during this session.
- Clarified product direction for the redesigned project detail flow: the earlier `Generate Report` tab prompt was the correct target, while the `Estimate` tab only needs a placeholder for now rather than the full report-style workflow.
- Applied a PDF-only visual upgrade in `backend/processing/report_generator.py` for the assessment report without changing section order, content structure, or generation logic. The update added the refined cover treatment, branded interior headers/section dividers, shaded brief callouts, larger centered photos, restyled appendix pages, branded condition schedule table, and text sanitization to strip em-dashes and exclamation points from rendered copy.
- Rebuilt the customer-facing portal route at `dashboard/app/portal/[id]/page.tsx` after it had been removed, restoring a branded report-first customer experience with published report preview, project photo gallery, estimate accept/decline/request-changes actions, proposal area after approval, and cleaner help/contact framing.
- Pulled the real Scottish Stained Glass logo assets from `https://scottishstainedglass.com/` and replaced placeholder wordmarks across the app and PDFs. Local assets now live in `dashboard/public/brand/ssg-logo-white.png` and `dashboard/public/brand/ssg-logo-color.png`.
