## Supabase handoff for `ssg-platform`

- Worked in `/Users/scottishgroup/Desktop/ssg-platform/backend` to finish Supabase-ready backend changes.
- Implemented and locally verified support for Supabase Postgres plus Supabase Storage S3-compatible uploads.
- Added/verified migration support in `scripts/migrate_to_supabase.py`.
- Confirmed backend still compiles and restored local/default config after Supabase DB auth failed, so the app was not left broken.
- In Supabase project `ndsedmdexbwpfxjtyamh`, created storage bucket `ssg-platform` and generated S3 credentials.
- Saved collected storage-side config in `/Users/scottishgroup/Desktop/ssg-platform/backend/.env.supabase.pending`.
- Remaining blocker: the working Supabase Postgres password is still needed. Supabase would not reveal the original password, and attempted resets did not authenticate successfully from SQLAlchemy.
- Once the correct DB password is available, remaining steps are to write the final backend env, switch the backend to Supabase, run the migration, and verify end-to-end reads/writes.
