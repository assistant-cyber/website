- Current direction for the Scottish Stained Glass app: remove the existing test/demo/stained-glass placeholder projects and replace them with real customer projects. Treat current seeded/sample project data as disposable, not production truth.
- First real replacement project: Trinity Episcopal Church, 93 Jeffers Road, Ennis, Montana 59729.
- Next real project: St Augustine Catholic Church, 175 2 6th Ave, Brighton, CO 80601.
- Next real project: Grace Presbyterian, 1515 W Charleston Blvd, Las Vegas, NV 89102.
- Cleaned the live `ssg-platform` backend database so the app now contains only those three real projects. Removed all prior sample/test projects plus their photos, condition data, estimates, reports, proposals, and customer users. Cleared `backend/uploads` and `backend/reports`. Recoverable backup saved under `/Users/scottishgroup/Desktop/ssg-platform/backend/_cleanup_backups/20260528-085131`.
- In `/Users/scottishgroup/Desktop/ssg-platform`, the report workflow was shifted to an AI-first draft flow: `Write Full Report` generates the editable customer-facing report copy, then `Build PDF` generates the final PDF from the reviewed draft.
- The report writer should always use a very professional default tone; the earlier voice-picker UI was removed by request.
- Mobile photo capture was confirmed in the dashboard photo flow: the phone-facing `Take Photo` control uses an image input with camera capture support, intended to open the device camera or expose camera capture through the phone picker.
- `Build PDF` now also auto-publishes the finished report package to the customer portal when the background build completes; the separate manual portal-send step was removed from the report tab.
- Report-writing/PDF expectations going forward: keep using the same house PDF style guided by `/Users/scottishgroup/Desktop/Sample report.pdf`, and have report prose reference concrete photo labels like `1A`, `2B`, etc. so users can tie findings to specific images.
- The PDF cover/report polish work now includes preserving the real logo aspect ratio, using a less-squished Appendix 4 layout, and making `Appendix 4: Window Condition Schedule` editable during report review before the PDF is built.
- The customer portal was simplified to match the stronger dashboard heading style and now skips the old `Step 1 / Step 2 / Step 3` explainer cards. The portal should focus on real customer-facing content first: assessment report, assessment photos, and contact info. Estimate/proposal content remains conditional and can be expanded later.
- Photo upload work on the dashboard shifted toward mobile reliability and speed: browser-side resize/compress before upload, JPEG normalization when needed, and removal of the long per-photo queue delay. If phone uploads still fail after these changes, the next likely fault domain is backend/storage rather than the browser flow.
- Photo management requirements now include batch selection plus zip download from both project-level `Photos` and workspace-level `All photos` views. Downloaded filenames should prioritize the field numbering label first, e.g. `12A.jpg`, with duplicate-safe suffixes like `_2`.
- Field photo numbering was redesigned into a window-sequence workflow: saying or typing `window 1 ...` anchors the next uploaded photo as `1A`, subsequent blank follow-up photos continue as `1B`, `1C`, etc., and the next explicit window note such as `window 2 ...` resets the sequence to `2A`. Backend normalization should preserve this behavior even when notes are edited later.
- The dashboard photo queue now supports mobile voice-to-text notes for field use so technicians can dictate the window identifier and issue description while uploading.
- The photo viewer behavior was upgraded across both project `Photos` and workspace `All photos`: clicking a photo should open a gallery-style viewer with left/right navigation, swipe on mobile, in-view download, and a right-side metadata panel showing project name plus window/elevation/notes when present. Users should be able to move through the whole set without exiting back to the grid each time.
