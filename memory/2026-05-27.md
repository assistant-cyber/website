## 2026-05-27

- Worked in `/Users/scottishgroup/Desktop/ssg-platform/dashboard` on the Scottish Stained Glass dashboard and customer portal.
- The local Next.js dev server repeatedly blank-screened because `.next` became corrupted and served missing chunks (for example `Cannot find module './548.js'`). Reliable fix: move or clear `.next` and restart `next dev`.
- Added auth/error handling on dashboard project list and project detail pages so `401 Not authenticated` redirects to `/login` instead of rendering a white screen; non-auth load failures now show a visible error state.
- Dashboard `/projects` layout was simplified per feedback: recent projects back to a horizontal arrangement, `New Project` kept on one line, and extra explanatory/sidebar marketing copy removed.
- Customer portal at `app/portal/[id]/page.tsx` was redesigned to a more modern business-professional look while keeping the same functionality and content, using Scottish Stained Glass green as a restrained accent rather than a dominant background.
